#include <stdio.h>
#include <stdbool.h>

/*打印一定范围内的质数*/

#define NUMBER 1000
int main(int argc, char const *argv[])
{
	bool isprime ;
	int primes[NUMBER] = (2,3);


	for (int i = 5; i < NUMBER; ++i)
	{
		if ((i % 2)!= 0)
		{
			isprime = true;

			for (int i = 0; i < count; ++i)
			{
				/* code */
			}
		}
	}
	return 0;
}